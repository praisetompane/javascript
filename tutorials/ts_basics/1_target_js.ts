/*
  user: TypeScript compiler's default JavaScript version is 3
        use tsconfig.json to configure a target that supports new features.
*/
async function hello() {
  return "world";
}

const url = new URL();
